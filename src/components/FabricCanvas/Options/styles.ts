import styled from "styled-components";

export const S = {
  Button: styled.button`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  `,
  Color: styled.div`
    position: absolute;
    top: 40px;
    left: 120px;
    background: white;
    border: 1px solid #ccc;
    padding: 10px;
  `,
};
