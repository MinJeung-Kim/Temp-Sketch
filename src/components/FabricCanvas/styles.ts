import styled from "styled-components";

export const S = {
  Canvas: styled.div`
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    height: 100%;
    overflow: auto;
  `,
};
